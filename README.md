# Java app launcher
This script loads environment variables into the shell's environment then launches the java app in that context.
# Use case
1. Run a java app but load ceratain values into the shell's environment first e.g. secret keys, passwords, ports e.t.c.